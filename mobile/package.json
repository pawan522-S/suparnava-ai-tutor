{
    "name": "suparnava-mobile",
    "version": "1.0.0",
    "description": "Suparnava AI English Tutor — Android App (Capacitor)",
    "private": true,
    "scripts": {
        "sync": "npx cap sync android",
        "open": "npx cap open android",
        "build": "npx cap sync android"
    },
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/core": "^6.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^6.0.0"
    }
}